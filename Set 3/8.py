#creates empty list
list = []

#requests input of number of elements to append to list
n = int(input("Enter number of approximations: "))

#range (0,n) iterates till the range of n
for i in range(0, n):
    ele = int(input())
    ele(10)
    list.append(ele)


print(list)
